import { useMemo } from 'react';
import { createGenerator,RARITY } from '@/game/data/generation';
export default function UpgradeScreen({run,onPick}){const choices=useMemo(()=>{const g=createGenerator(run.seed+run.level*3571+17);return[g.passive(run.luck),g.passive(run.luck),g.passive(run.luck)]},[]);return <main className="game-grid flex min-h-screen items-center justify-center p-5 text-white"><section className="w-full max-w-4xl"><div className="text-center"><p className="font-black uppercase tracking-[.3em] text-cyan-200">Signal Acquired</p><h1 className="mt-2 text-4xl font-black uppercase sm:text-5xl">Choose an Upgrade</h1><p className="mt-2 text-slate-300">Procedurally approved professional development.</p></div><div className="mt-8 grid gap-4 md:grid-cols-3">{choices.map(u=><button key={u.id} onClick={()=>onPick(u)} className="min-h-60 cursor-pointer rounded-3xl border-2 bg-slate-900/90 p-6 text-left hover:bg-slate-800 focus-visible:outline focus-visible:outline-4 focus-visible:outline-white/60" style={{borderColor:RARITY[u.rarity].color}}><span className="text-5xl">{u.icon}</span><div className="mt-4 text-xs font-black uppercase" style={{color:RARITY[u.rarity].color}}>{u.rarity}</div><h2 className="mt-1 text-2xl font-black text-white">{u.name}</h2><p className="mt-2 font-bold text-cyan-200">{u.desc}</p>{u.special&&<p className="mt-3 text-sm text-amber-200">{u.special}</p>}</button>)}</div></section></main>}